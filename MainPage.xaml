<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.TreeView;assembly=Syncfusion.Maui.TreeView"
             x:Class="MauiLab.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <syncfusion:SfTreeView BackgroundColor="White" x:Name="FolderList" ItemsSource="FolderItems" ItemTapped="FolderListOnItemTapped" ExpandActionTarget="Node" FullRowSelect="True" IsAnimationEnabled="True">
                <syncfusion:SfTreeView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5,0,0,0">
                            <Label Text="{Binding name}" VerticalTextAlignment="Center" TextColor="Black"/>
                        </Grid>
                    </DataTemplate>
                </syncfusion:SfTreeView.ItemTemplate>
            </syncfusion:SfTreeView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
