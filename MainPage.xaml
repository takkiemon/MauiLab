<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fa="clr-namespace:MauiLab;assembly=MauiLab"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:tabs="http://sharpnado.com"
             x:Class="MauiLab.MainPage"
             Shell.TabBarIsVisible="True"
             Shell.BackgroundColor="LightCoral"
             Shell.ForegroundColor="Yellow"
             Shell.TitleColor="DarkBlue">

    <ScrollView
            VerticalOptions="Fill">
        <VerticalStackLayout
            Padding="30,0"
            VerticalOptions="Fill"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            
            <!--  Example of segmented tab bar (rounded floating tabs)  -->
            <tabs:TabHostView Grid.Row="2"
                    WidthRequest="280"
                    HeightRequest="60"
                    Margin="15"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill"
                    CornerRadius="30"
                    IsSegmented="True"
                    TabType="Fixed"
                    SelectedIndex="{Binding Source={x:Reference Switcher}, Path=SelectedIndex, Mode=TwoWay}">
                <tabs:TabHostView.Tabs>
                    <tabs:BottomTabItem x:Name="FirstTab"
                                        IconImageSource="house_96.png" 
                                        Label="labelly">
                        <tabs:BottomTabItem.Badge>
                            <tabs:BadgeView x:Name="TargetBadge"
                                BackgroundColor="White"
                                TextColor="DarkGoldenrod"
                                            HorizontalOptions="End"
                                            VerticalOptions="Start"
                                            RotationY="0"
                                            RotationX="0"
                                            Rotation="45"
                                            Text="&#xf021;"
                                            TextSize="35"
                                FontFamily="FontAwesome" />
                        </tabs:BottomTabItem.Badge>
                    </tabs:BottomTabItem>
                    <tabs:BottomTabItem x:Name="SecondTab"
                                        IconImageSource="list_96.png" 
                                        Label="Checkity">
                        <tabs:BottomTabItem.Badge>
                            <tabs:BadgeView BackgroundColor="DodgerBlue"
                                BadgePadding="4,2"
                                TextSize="13"
                                Text="Billy" />
                        </tabs:BottomTabItem.Badge>
                    </tabs:BottomTabItem>
                    <tabs:BottomTabItem x:Name="ThirdTab"
                                        Routing.Route="pokemon"
                                        IconImageSource="grid_view_96.png" 
                                        Label="testou toastou">
                        <tabs:BottomTabItem.Badge>
                            <tabs:BadgeView Margin="20,5"
                                BackgroundColor="Coral"
                                ShowIndicator="True"
                                TextSize="14"
                                Text="3" />
                        </tabs:BottomTabItem.Badge>
                    </tabs:BottomTabItem>
                </tabs:TabHostView.Tabs>
            </tabs:TabHostView>
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>
